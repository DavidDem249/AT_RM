/**
 * First we will load all of this project's JavaScript dependencies which
 * includes Vue and other libraries. It is a great starting point when
 * building robust, powerful web applications using Vue and Laravel.
 */

require('./bootstrap');

var Turbolinks = require("turbolinks")
Turbolinks.start()


/*
import 'fullcalendar/dist/fullcalendar.css';
import FullCalendar from 'vue-full-calendar';


window.Vue = require('vue');

import Vue from 'vue/dist/vue';
import VueRouter from 'vue-router';
Vue.use(VueRouter);
// Vue.use(FullCalendar);

// Vue.component('calendar-component', require('./components/events/EventComponent.vue').default);
// Vue.component('dash-board', require('./components/DashboardComponent.vue').default);

import Home from './components/DashboardComponent.vue';
import Profile from './components/ProfileComponent.vue';
import Ressource from './components/ressources/RessourceComponent.vue';
// import Event from './components/events/EventComponent.vue';

const routes = [
	{
		path: '/dashboard',
		component: Home
	},

	{
		path: '/dashboard/profile',
		component: Profile
	},

	{
		path: '/dashboard/ressources',
		component: Ressource
	},
	
];

const router = new VueRouter({
	mode: 'history',
	routes,
});


const app = new Vue({
    el: '#app',
    router: router
});
*/